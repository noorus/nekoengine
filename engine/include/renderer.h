#pragma once
#include "neko_types.h"

namespace neko {

  /*using GLTextureHandle = GLuint;
  using GLFramebufferHandle = GLuint;
  using GLRenderbufferHandle = GLuint;

  class Texture {
  public:
    enum Type {
      Tex1D,
      Tex2D,
      Tex3D,
      Tex1DArray,
      Tex2DArray,
      Tex3DArray,
      TexCubemap,
      TexCubemapArray,
      Tex2DMultisample,
      Tex2DMultisampleArray
    };
    enum Mode {

    };
  protected:
    //GLenum id;
  };

  using TexturePtr = shared_ptr<Texture>;

  class Renderer {

    //
  };*/

}